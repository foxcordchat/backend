generator client {
  provider = "dart run orm"
  output   = "../lib/generated/structure/prisma/client.dart"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id BigInt @id
}
